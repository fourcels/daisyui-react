import { Collapse, CollapseProps } from "../Collapse";

export type AccordionItemProps = CollapseProps;

export const AccordionItem = Object.assign({}, Collapse);

AccordionItem.displayName = "AccordionItem";
